# ex042: Polynomial SVM
### Non-Linear Data
Many datasets can't be split by a line. Adding polynomial features (x^2, x^3) often makes them separable in higher dimensions.
### Limitation
Adding real features is slow. Use the Kernel Trick for efficiency.
