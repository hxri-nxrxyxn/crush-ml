# ex016: ColumnTransformer
### Routing Data
You can't scale text or one-hot encode numbers. `ColumnTransformer` lets you apply different pipelines to different subsets of columns (numeric vs categorical) and merges the results into one perfect matrix.
