# ex106: Semantic Segmentation
Classifying every single pixel in an image. Uses **Fully Convolutional Networks (FCNs)** where pooling layers are replaced by **upsampling** layers (`Conv2DTranspose`) to restore the original resolution.
