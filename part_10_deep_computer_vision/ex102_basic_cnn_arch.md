# ex102: Basic CNN Architecture
A standard CNN follows a repetitive pattern: `[Conv -> ReLU -> Pool] x N -> Flatten -> Dense -> Softmax`. As you go deeper, image size decreases while depth (number of filters) increases.
