# ex104: Pre-trained Models
Keras provides state-of-the-art models (ResNet, Xception, MobileNet) pre-trained on ImageNet. Using `include_top=False` lets you keep the feature extraction part and add your own classification head.
